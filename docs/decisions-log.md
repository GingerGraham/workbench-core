# workbench-core — decisions log

Numbered decisions referenced from `docs/architecture.md` §12. Entries are
append-only: never renumber or rewrite an existing one. If a decision is
superseded, add a new entry that says so and leave the old one as-is.

| # | Decision | Resolution |
|---|---|---|
| D1 | SSH management placement | Separate `workbench-ssh` module for the opinionated layer; bootstrap-critical deploy-key/alias mechanics live in `workbench-core` itself, since they're needed for *any* private repo before a module could be synced. |
| D2 | Profile concept in a modular world | Hybrid: no hardcoded profile branching in the engine (module set registered is the real state), but named **bundles** — pure sugar over `wb add` — give back the one-command convenience. Requires a known-modules catalog (§10), living in `workbench-core`. |
| — | CLI naming (surfaced by D2) | `workbench-core` ships the `wb` CLI from Wave B. `workbench` is reserved for a separate TUI repo (Wave D) that fronts `wb`, not a competing command surface. |
| D3 | `workbench-precursor`'s fate | Donor-then-archive — extract pieces into the new repos, then archive it. No in-place repurposing. |
| D4 | Windows/PowerShell scope | Out of scope. Linux/macOS/WSL2 only, matching `dotfiles`' current reach. |
| D5 | Distribution mechanism | Production tracking (`latest`/`tag:`/`commit:`) never leaves a persistent, incrementally-`git pull`ed working copy. Public repos: no `git` at all — GitHub API resolution + tarball fetch. Private repos (independent tools only — no `workbench-*` repo is ever private, §2): `git` as one-shot transport only — a shallow `git clone --depth 1` into a scratch directory, extracted, `.git` discarded — because GitHub does not support the `git archive --remote` protocol (verified; GitLab does). Dev tracking (`branch:`) uses the same shallow-clone-and-discard primitive, refreshed every cycle, by design the only pathway that invokes `git` as a matter of course rather than platform necessity. See §9.1. |
| D6 | Tracking model | Four `TRACK_MODE` states — `latest` (default), `branch:<name>`, `tag:<name>`, `commit:<sha>`. Tag format contract: `vX.Y.Z` only participates in `latest` resolution; pre-release tags are valid explicit-pin inputs but excluded from automatic tracking. Semver comparison is pure bash, bash-3.2-compatible, generalised from `install.sh`'s existing `version_ge()`. See §9.2. |
| D7 | Tracking persistence & the module contract | Machine-side `sync.conf` (`TRACK_MODE`/`TRACK_REF`) is authoritative; the manifest's `branch:` field is a default only, superseded permanently once machine state exists. Loader exports a derived, read-only `WORKBENCH_TRACK_<MODULE>` into the interactive shell, sourced from `sync.conf`. Published now as a standing Core API contract for Wave C modules to build against. See §9.5. |
| D8 | Sync cadence | Single shared timer (principle 4 unchanged — not true per-module differential cadence). Weekly by default; drops to 5 minutes globally for as long as any module is `branch:`-tracked, reverting once none are. Manual on-demand trigger (`wb update`) always available regardless. See §9.4. |
| D9 | `wb dev` shape | Guided interactive wrapper over a single `wb track` verb, not a separate mechanism or a `wb dev`/`wb prod` verb pair. See §10. |
| D10 | Canonical `ARCHITECTURE.md` location vs. the `workbench-precursor` pointer note | Copied here per Phase 0 as planned. The reciprocal one-line pointer note in `workbench-precursor`'s own copy (`workbench-core-build-brief.md` Phase 0) was **not** applied by this build pass — `workbench-precursor` is treated as strictly read-only donor material for Wave B (per the build brief's non-negotiables), and the repo owner asked that the pointer note be tracked as a separate follow-up rather than committed here. Action item: add `> Canonical copy: workbench-core/ARCHITECTURE.md` (or similar) to the top of `workbench-precursor/ARCHITECTURE.md` in a dedicated pass before/at Wave C's `workbench-precursor` retirement. |
| D11 | `workbench-core` repo visibility timing | Ship notes assumed `workbench-core` would be created public from the start; in practice the repo existed privately going into this build and was flipped to public by the repo owner directly (GitHub UI) partway through Phase 0, ahead of the initial commit landing. No code or CI in this repo depends on visibility, so no other changes were needed. |
| D12 | Private-repo/dev shallow-clone form | `git init` + `git remote add` + `git fetch --depth 1 origin <ref>` + `git checkout FETCH_HEAD`, rather than `git clone --depth 1 --branch <ref>` directly. This form works uniformly whether `<ref>` is a branch name, a tag, or a full commit SHA (a depth-1 clone by SHA is rejected by stock GitHub unless the server has `uploadpack.allowReachableSHA1InWant` enabled, which is not guaranteed) — see `lib/distribution/fetch-git-snapshot.sh`. |
| D13 | Prereq list refinement: `ssh-keygen` | §7 item 1's enumerated prereq list (`awk`, `sed`, `tr`, `grep -E`, `column`, `git`, `curl`, `ssh-keyscan`, optionally `gpg`) names `ssh-keyscan` but not `ssh-keygen`, even though Phase 6's deploy-key generation (`lib/ssh/bootstrap.sh`) hard-depends on the latter. Added `ssh-keygen` to `lib/core/prereqs.sh`'s required list alongside `ssh-keyscan` — both ship in the same `openssh-client`(`s`) package on every distro checked, so this adds no new install-mapping complexity, just an explicit check for a binary the original list should probably have named directly. |
| D14 | Snapshot atomic-swap primitive | `ln -sfn` (GNU) with a fallback to `ln -sfh` (BSD/macOS, since GNU coreutils 9.4 as tested does not recognise `-h`, and BSD `ln` does not recognise `-n`) directly against the final `current` path — not a temp-symlink-then-`mv`. `mv src dest` treats an existing symlink-to-directory `dest` as a directory to move *into* rather than replacing it, which would silently nest the new snapshot inside the old one; this was caught by `tests/check-snapshot-atomicity.sh` during the build (the temp+`mv` form initially implemented left `current` stuck on the very first snapshot forever) and fixed to match the direct-`ln` primitive `workbench-precursor`'s own release-mode swap already used successfully. See `lib/distribution/snapshot.sh`. |
| D15 | `deploy.list`/`register.list`/`hooks.list` as static rendered artifacts | Only `register.list` is materialised on disk (written by the sync engine after every successful fetch, from the module's manifest in `current`) — `deploy.list` and `hooks.list` are NOT pre-rendered separate files; `lib/sync/engine.sh` reads deploy and hook entries live from the manifest via `lib/manifest/parse.sh` (already pure bash/awk, already hot-path-safe) at the moment they're needed. This simplifies the brief's original three-file-per-module shape to one, without reintroducing a `yq`/`python3` dependency or a second source of truth to keep in sync with the manifest — `ansible/roles/module_sync` accordingly does not render these files itself; it only invokes `wb add`, which triggers the same live read. |
| D16 | `register.shell[]` destination shape (supersedes §5.2's `modules.d/` sketch) | `register.list` entries point directly at `<module>/current/<src>` — the file exactly where it already lives inside that module's own fetched snapshot — rather than a separate `${WORKBENCH_HOME}/modules.d/<module-name>/<basename>` symlink tree. Since every registered file is read straight out of `current` at source time (`lib/loader.sh`), an extra symlink layer would only reproduce the same path with more moving parts; the "engine-computed, not author-supplied, and structurally confined to the module's own namespace" property §5.1/§5.2 cares about holds identically either way. Caught during PR review (a doc/implementation mismatch flagged against `contracts/manifest-spec.md`) rather than found during the build itself — logged here so the two don't drift again. |
| D17 | Bootstrap fetch mechanism | `bootstrap.sh` (repo root) is the canonical, documented production install path — curl + tar, no `git`, resolving the latest release tag and fetching straight into the real `snapshots/<ref-slug>-<shortsha>/` path (never a scratch/temp directory), then handing off to `bin/wb install`. It duplicates a small, deliberate slice of `lib/distribution/resolve.sh`/`fetch-tarball.sh`/`snapshot.sh`'s slugify — structurally unavoidable, since bootstrap can't depend on code it hasn't fetched yet — the same narrow-exception precedent as the private-repo git-as-transport case (§9.1/D5). Falls back to `main` when no `vX.Y.Z` tag exists yet, permanently (not just for bring-up). Hosted unversioned at `main`, unlike every tag-tracked module — it has no persistent state of its own to drift. The developer `git clone` + `./bin/wb install` path is unchanged and still correct (§9.6); it's relabelled "Developer setup" in the docs rather than removed. See §9.1a. |
| D18 | Script versioning | Three independent, decoupled version concepts: contract versions (§6, unchanged), a new release/tag version (`VERSION` file at the repo root, `vX.Y.Z`, read via `workbench_release_version()`), and a new script-local version (one `X.Y.Z` per operational file — `bin/wb`, `bootstrap.sh`, `lib/loader.sh`, every file under `lib/`; not `tests/` or the Ansible playbooks/roles). Script-local versions are tracked via a plain indexed array (`_WB_SCRIPT_VERSIONS`, `workbench_register_script_version()`/`workbench_print_script_versions()` in `lib/core/version.sh`), not a repeated same-named variable — the latter would get clobbered file-to-file across `bin/wb`'s ~20-file single-process source pass (or throw, if `readonly`); the array pattern mirrors `_WB_SHELL_PREREQS_REQUIRED`'s existing convention. `wb version` is a new subcommand surfacing both the release version and the full script-version list. See §6.1. |
| D19 | `wb` CLI exposure on `PATH` | `wb install`/`wb apply` symlinks `~/.local/bin/wb` → `<core>/current/bin/wb` (`_wb_link_cli_bin`, `bin/wb`), non-destructive on collision. `lib/loader.sh` additionally guarantees `~/.local/bin` is prepended to `PATH` on every shell start, unconditionally — distro/shell rc-file conventions for this are inconsistent (absent on stock macOS zsh, conditional-on-existence on Debian/WSL) and can't be relied on alone. Confirmed gap: no mechanism for this existed anywhere in the Wave B build — `bin/wb` was only ever invoked internally by full path (`get-functions()`). |
| D20 | Core self-convergence on update | `wb update`/`wb track`/`wb dev` targeting `core`, and the background timer's `wb sync run-if-due`, now detect when `core`'s resolved commit actually changed and automatically re-run `wb apply` afterward — so a `core` update always leaves the host fully converged (PATH symlink, rc stub, prereqs, SSH bootstrap, Ansible) without a separate manual step. Scoped to `core` only (principle 4 — no special-cased branch inside `lib/sync/engine.sh`; the new logic lives at the CLI layer, `_wb_maybe_reconverge_core` in `bin/wb`, called from `_wb_cmd_update`, `_wb_cmd_sync`'s `run-if-due` branch, and `workbench_cmd_track`) — the same question for ordinary modules is explicitly deferred, since none has an install/apply concept today. Only fires on an actual commit change, not every cycle — a no-op sync cycle never pays the convergence cost. The Ansible pass specifically is skipped when triggered from the unattended timer path (`--skip-ansible`, a new flag on `wb install`/`wb apply`, also usable directly by a human for a faster apply), preserving §8's "unattended stays pure shell, no Ansible" principle for that one component; everything else in convergence — prereq installs and SSH bootstrap included — now legitimately runs unattended for `core`, since both are already idempotent/non-interactive and core's own PATH-symlink/rc-stub convergence needs them to actually converge. A convergence failure is logged as a warning and never fails the triggering command — same non-fatal pattern already used for post-deploy hook failures (`workbench_run_post_deploy_hook`). |
| D21 | `register.list`/`installers.list` convergence on `wb install`/`wb apply` (baseline-completion brief §Phase 1) | A confirmed regression: `bootstrap.sh`-driven installs never rendered core's own `register.list` at all, because `_wb_bootstrap_core_module`'s early-return (correctly reached once `bootstrap.sh` has already registered core) skips the `workbench_render_register_list` call further down in that same function's body, and nothing else in `wb install`/`wb apply`/`wb update` called it unconditionally — only a genuine upstream commit change did, via the sync engine's fetch path. Net effect: a `bootstrap.sh`-installed host silently never got core's own shell content (`get-functions`, `sudo-test`, `get-elevation-command`, `dedupe-path`, etc.), with no error anywhere. Fixed by making `register.list`/`installers.list` rendering an unconditional, idempotent step of `wb install`/`wb apply` (`_wb_converge_module_registrations` in `bin/wb`) for **every** loadable module, every run, regardless of whether anything changed — one general fix (principle 4), not a core-specific patch. `wb status` additionally flags loudly (not silently) a registered, sync-enabled module whose manifest declares `register.shell[]`/`register.installers[]` entries but whose `register.list` is missing or empty. |
| D22 | Local overrides directory (baseline-completion brief §Phase 2) | `~/.config/workbench/local/` replaces the single-file `90-local.sh` — one reserved-name `settings.sh` keeping today's early+final two-pass semantics, plus any number of other user-authored `.sh` files sourced together, filename-sorted, immediately after `settings.sh`'s final pass. `WORKBENCH_USER_EXT_DIR` is unchanged and stays the true last-of-everything, kept conceptually distinct from personal overrides — sourced after the new "other files" pass, per the repo owner's steer that it should continue to be sourced last. `wb install`/`wb apply` scaffold the directory and a default, commented `settings.sh` (never overwriting an existing one). While reworking this block, fixed a latent ordering bug this brief's own done-when criteria exposed: `lib/loader.sh`'s "Behaviour flags" block (which reads `WORKBENCH_PLAIN_SHELL` to force `WORKBENCH_SHOW_FUNCTIONS=false`) previously ran *before* the local-overrides file was ever sourced, so a `WORKBENCH_PLAIN_SHELL=true` set only there (never as a real pre-existing environment variable) correctly gated the later prompt-fallback block but silently failed to gate `WORKBENCH_SHOW_FUNCTIONS` — the early source now runs first, so both are gated consistently, matching what the file's own comments already claimed. See §9.3/contracts/state-schema.md. |
| D23 | Tool-updating framework (baseline-completion brief §Phase 3) | `wb tools` (not `wb update-tools`, not folded into `wb update`) is the command surface for a genuine design gap: `register.installers[].src` was declared/parsed/validated from Wave B onward but nothing ever consumed it at runtime. Discovery is by `install-<name>` naming-convention introspection over each declared file's plain text (`_extract_function_names`, `lib/core/functions.sh` — the same primitive `get-functions` already uses, never a second parser), rendered into a new per-module `installers.list` (`workbench_render_installers_list`, `lib/sync/engine.sh`) at exactly the same points `register.list` is rendered — not a new per-tool manifest declaration. `wb tools list`/`wb tools update [<name>]` (`lib/core/tools.sh`'s `workbench_tools_collect`/`workbench_tools_lookup`, consumed by `bin/wb`) discover and invoke; update logic, idempotency, and version-checking are entirely the module author's responsibility — core calls the function and reports what it says, never tracking installed-tool versions or diffing state itself. Manual-only, no cadence/timer integration, and excluded for disabled modules exactly like `register.list`. A friendly-name collision across modules (two `install-terraform` functions, say) is resolved by first-by-module-name-order-wins, the same tie-break the loader's tier-sourcing already uses, warned about once. `STATE_SCHEMA_VERSION` bumped `1` → `2` for the new `installers.list` file, migrated in place by `wb install`/`wb apply` (`workbench_migrate_state_schema`, `lib/core/version.sh`) even though the change is purely additive. |
| D24 | Internal Core API function naming convention | `lib/core/version.sh`'s twelve version/schema-bookkeeping functions and `lib/core/functions.sh`'s `workbench_detect_platform` — thirteen in total — were never meant to be invoked from a prompt or appear in `wb functions`/`get-functions` output, but weren't `_`-prefixed, so once that listing actually walked and extracted real function names they showed up as noise next to genuinely user-facing commands (`sudo-test`, `dedupe-path`, etc.). Renamed all thirteen (e.g. `workbench_detect_platform` → `_workbench_detect_platform`) to the `_`-prefixed convention `_read_prompt`/`_read_prompt_silent` already established; none are named in the `CORE_API_VERSION` contract paragraph, so this is not a Core API surface break and required no version bump — purely a display/discovery convention, since `_`-prefixing a bash function doesn't restrict who can call it, only whether `_extract_function_names` lists it. |
| D25 | Prereq list addition: `unzip`/`zip` | Added `unzip` and `zip` to `_WB_SHELL_PREREQS_OPTIONAL` in `lib/core/prereqs.sh`, ahead of any Wave C module actually needing them — anticipated for modules distributing `.zip`-packaged tools (Ubuntu does not ship `unzip` by default). Optional, not required, matching the existing `gpg` precedent: nothing in core itself calls either binary. No new package-name mapping needed in `workbench_install_shell_prereqs`'s case statement — binary and package names match on every checked package manager (apt/dnf/yum/zypper/pacman/brew), so both fall through to the existing default branch. |
| D26 | CI test/lint pipeline | `.github/workflows/ci.yml`: shellcheck lint (single run) + the existing `tests/check-*.sh` suite (all 22, matrix across `ubuntu-latest`/`macos-latest`), triggered on PR-open to `main` and manual dispatch, also exposed via `workflow_call` so `release.yml` (D27) depends on it rather than duplicating a test run. `check-distribution-no-git.sh` is pointed at the PR's own branch/SHA in CI via `WORKBENCH_TEST_GH_BRANCH`, not left at its `main` default — a PR run must validate the code under review, not always re-validate `main`. The exact `shellcheck bin/wb bootstrap.sh lib/**/*.sh tests/*.sh .github/scripts/**/*.sh` invocation (no `.shellcheckrc`, no severity flag) surfaced a pre-existing backlog across the codebase — 26 real warnings (unguarded `cd`, unquoted expansions, unused variables, a couple of genuine array/string mix-ups) and ~105 stylistic/info notes (almost all the same `cond && ok || fail` idiom the test suite's own `ok()`/`fail()` convention relies on everywhere, which is safe here since neither function can itself fail). Rather than relaxing severity or adding a `.shellcheckrc`, the warnings were fixed in place and the info-level idiom was suppressed with inline `# shellcheck disable=` comments at each site, matching the codebase's existing annotation convention — so lint runs clean at full severity with no new suppression mechanism. Deliberately phase one of a larger planned structure — a black-box bootstrap/install test and a synthetic-fixture ecosystem-readiness test (full Core API surface, since no extension modules exist yet) remain a separate follow-up, not built here. |
| D27 | Release automation | Two GitHub Actions workflows: `pr-check.yml` validates Conventional-Commit-formatted messages on any commit touching a registered script-local-versioned file; `release.yml` runs on every merge to `main` (guarded against re-triggering on its own bot commit), deriving per-file and overall bump severity from commit history since the last tag (`.github/scripts/release/`), gated on a non-empty `CHANGELOG.md` `[Unreleased]` section. `core`-scoped commits (no corresponding file) can independently drive the overall `VERSION` bump above whatever the per-file rollup alone would produce. `release.yml`'s own test-suite safety net is `ci.yml` (D26) via `workflow_call`, not a duplicated inline run. First tag cut manually as `v1.0.0` (§7 of the release brief) since Wave B is functionally complete; the automation requires an existing baseline tag to diff against. Resolved the `bootstrap.sh`/D18 mismatch (bootstrap.sh had no registration call despite being listed in scope) by adding one that references its existing `_WB_BOOTSTRAP_VERSION` variable rather than a second literal version string, so there's still exactly one source of truth for that file's version. See `docs/release-process.md`. **Superseded in part by D28**: `release.yml`'s original direct-push-to-`main` mechanism described here was replaced before this ever shipped, once `main`'s actual branch ruleset (no bot/App bypass) made a direct push unworkable — the bump computation, CHANGELOG gate, and per-file rewrite logic described above are unchanged; only how the result lands on `main` changed. |
| D28 | Release pipeline: PR + auto-merge instead of direct push to `main` | `main`'s branch ruleset (require PR, require signed commits, require linear history, require status checks, block force pushes; no bot/App bypass entry) makes a direct-push release step (D27's original design) unworkable without weakening the ruleset — not an option. Resolved by splitting `release.yml` into a propose step (computes the version bump exactly as D27 describes, then opens a PR from `release/v<VERSION>` to `main`) and a new `release-finalize.yml` (tags the resulting squash-merge commit and publishes the GitHub Release once that PR merges). The PR is opened and auto-merge enabled using a dedicated GitHub App installation token (`RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY`, `actions/create-github-app-token`) rather than `GITHUB_TOKEN` — not to bypass any rule, but because events triggered by `GITHUB_TOKEN` don't cascade to trigger other workflows, so a `GITHUB_TOKEN`-opened PR would never fire `ci.yml` (D26) and auto-merge would wait forever. Every ruleset requirement is satisfied by the normal PR path (GitHub signs its own merge commit; squash-merge preserves linear history); `main`'s bypass list stays untouched — Repository admin only, no App exception added. The `chore(release):` self-trigger guard moved from an actor check to a commit-message-prefix check, since the actor completing an auto-merge isn't reliably attributable to a known bot identity the way a direct `GITHUB_TOKEN` push was. Whether a separate tag-ref protection ruleset applies to `v*` tags (distinct from the `main` branch ruleset) was flagged back rather than assumed either way — if one exists, the App needs adding to that bypass list specifically, a narrower grant than the `main` bypass this decision deliberately avoids touching. |
| D29 | `CORE_API_VERSION` granularity | Widened from a bare integer to `X.Y` (`1` → `1.0`) so additive, non-breaking growth of the Core API surface (§6) can be signalled to modules via minor-version floors, without forcing every existing `core_api: ">=1.0 <2.0"` declaration to change — the range comparator already treats `1`/`1.0` as equal. Bump policy: minor for anything added to contracts/core-api.md's documented surface, major (minor resets to 0) for anything removed or behaviourally changed there. No STATE_SCHEMA_VERSION bump — the version file's shape is unchanged, only one value's precision widens. |
| D30 | `MANIFEST_SCHEMA_VERSION` enforcement | Closed a real gap: nothing in the hot sync path checked a manifest's own `version:` field — only the standalone, developer-time validator did, via a bare literal. Split into two fixes: the validator's literal becomes a named `_WB_MANIFEST_SCHEMA_VERSIONS_SUPPORTED` constant (readiness for the `version: 2` escape hatch, §5.4, no behaviour change yet); the sync engine gains an explicit gate in `workbench_sync_module`, refusing to sync a module (deploy and register both) whose declared `version:` isn't in that supported set, matching how the `core_api` gate already refuses loudly rather than silently guessing (principle 5). The validator's and the engine's constants are intentionally separate, unshared values — the validator must run standalone without a `workbench-core` install present, mirroring the accepted `bootstrap.sh`/`fetch-tarball.sh` duplication (D5's rationale). Checked against the freshly-fetched snapshot *before* `workbench_snapshot_swap`/`RESOLVED_SHA` persistence, not after like the `core_api` gate — a post-swap check would repoint `current` (and therefore every already-deployed symlink/register entry resolving through it) at unsupported content and then freeze `RESOLVED_SHA` on it, so refusal wouldn't actually keep the bad content off an already-synced module and the next cycle would short-circuit as "up to date" and never re-evaluate. Consequence: unlike `core_api`'s once-per-change frequency, this gate's `log_error` re-fires every cycle a mismatch persists (`RESOLVED_SHA` is deliberately never advanced past it) — a deliberate tradeoff, going quiet on a module stuck on an unsupported version being the worse silence. No new throttle/dedup mechanism added either way. |
| D31 | Repo governance files: PR template, issue templates, CODEOWNERS, CONTRIBUTING.md, SECURITY.md | The repo had automated commit/changelog/release discipline (D26–D30) but nothing documented or discoverable — a human or agent contributor had to reverse-engineer `pr-check.yml`/`release.yml` before opening a PR correctly. `CONTRIBUTING.md` and the PR template make the existing rules visible instead of tribal knowledge; issue templates (bug/feature/module-proposal) structure intake ahead of Wave C; `SECURITY.md` documents the private-vulnerability-reporting path given the repo's real trust-boundary surface (sourced shell content, `sudo`/`run0` elevation, SSH deploy keys). `CODEOWNERS` is documentary only for now — see D33. |
| D32 | Agent-instruction file topology | `AGENTS.md` holds the actual content (non-negotiables + pointers to `ARCHITECTURE.md`/`CONTRIBUTING.md`/`docs/`) once, matching this repo's own established pattern of short pointers over duplication. `CLAUDE.md` imports it via Claude Code's documented `@path` import syntax rather than restating it — Claude Code does not natively discover a root `AGENTS.md`. GitHub Copilot's various surfaces (CLI, coding agent, code review, Chat) each discover a different subset of `{AGENTS.md, CLAUDE.md, .github/copilot-instructions.md}`, so an adapted copy (same content, own preface, path-adjusted links) in `.github/copilot-instructions.md` is unavoidable duplication across tool-specific discovery paths, not a design choice — flagged so a future edit to `AGENTS.md` is remembered to be mirrored, not treated as optional. Two path-scoped `.github/instructions/*.instructions.md` files (Copilot-only mechanism) separate the Bash-3.2-constrained shell surface from the Bash-4+-exempt `.github/scripts/**` release tooling, since that nuance is easy for an agent to get backwards in either direction. |
| D33 | `CODEOWNERS` present but Code Owner review not required on `main`'s ruleset | With a single maintainer, requiring code-owner review on every PR would block Graham's own solo merges — GitHub does not allow self-approval to satisfy a required-reviewer rule for the PR's own author. `CODEOWNERS` still ships now (auto-assignment, and already correct the day a second maintainer joins); turning the ruleset requirement on is deferred until that happens, not decided here. |
| D34 | Install-helper functions are Core API surface, not per-module (Wave C kickoff) | `workbench-precursor`'s `installers-common.sh` (`_download_file_robust`, `_ensure_npm`, `_node_version_at_least`, `_npm_global_install`) is ported to `lib/core/installers-common.sh` and registered in core's own manifest at `tier: core`, rather than copied into each of `workbench-cloud`/`workbench-iac`/`workbench-containers`/`workbench-security`/`workbench-ai`/`workbench-devtools`/`workbench-desktop` independently — avoiding seven copies of the same helper file drifting apart, per the Wave C module map's own §7 call-out. Ported `_str_lower` (a confirmed gap: used by the precursor's `git.sh`/`gpg.sh`/`gpg-management.sh`/`installers-dev.sh`/`disk-encryption.sh` for case-insensitive comparisons, but never carried into `workbench-core`'s own `functions.sh` during Wave B) into `lib/core/functions.sh` at the same time, for the same reason — one shared, bash-3.2-safe implementation rather than each Wave C module reintroducing it. Both are new, documented Core API surface (`contracts/core-api.md`), so `CORE_API_VERSION` bumps `1.0` → `1.1` per D29's additive-growth policy — no `STATE_SCHEMA_VERSION`/`MANIFEST_SCHEMA_VERSION` effect. The known-modules catalog (`lib/modules/modules.list`/`bundles.list`, §10) is populated with real URLs for the four confirmed ecosystem modules (`workbench-git`/`workbench-gpg`/`workbench-ssh`/`workbench-shell`) as their own decomposition PRs land — the seven new domain repos from the Wave C module map are deliberately left uncataloged until each has its own decomposition PR, rather than pre-listed against repos with no manifest yet. |
| D35 | Known-modules catalog fully populated (Wave C completion) | The seven new domain repos D34 deliberately left uncataloged (`workbench-cloud`/`workbench-iac`/`workbench-containers`/`workbench-security`/`workbench-ai`/`workbench-desktop`/`workbench-devtools`) each landed its own decomposition PR, so `lib/modules/modules.list` now lists real, resolvable URLs for all eleven ecosystem modules under catalog names matching each module's own manifest (`cloud`/`iac`/`containers`/`security`/`ai`/`desktop`/`devtools`, alongside the four from D34) — the deferral condition D34 stated is met, not superseded. `lib/modules/bundles.list` is intentionally left unchanged: its three bundles mirror the original `dotfiles` monolith's three profiles for continuity (§10), and the seven new domain modules have no equivalent precedent there to mirror — adding them would be inventing new bundle membership decisions this task doesn't need to make, not completing a carried-over deferral. `workbench-precursor` itself is archived separately, outside this catalog change (non-negotiables, "frozen `dotfiles` .../`workbench-precursor` ... archived once its content is extracted") — extraction is what this decision confirms is now complete, archival is a repo-admin action taken independently. |
| D36 | Unconditional CORE_API_VERSION sync (host debugging session — gpg module registration permanently refused on an existing host) | A confirmed regression: `_workbench_ensure_version_file` correctly never overwrites an existing version file, but nothing ever advanced `CORE_API_VERSION` on an existing one either — a host bootstrapped before D34 shipped `CORE_API_VERSION=1.1` as the default stayed frozen at the bare `1` default through three subsequent core releases, permanently refusing any module whose declared `core_api` floor had since risen past that frozen value, with no self-healing path short of a manual file edit. Unlike `STATE_SCHEMA_VERSION` (whose migration asserts a claim about the shape of *other* on-disk files, and therefore needs a deliberate, sequenced step before advancing the number — D21's `installers.list` addition being the concrete example), `CORE_API_VERSION` states only what the currently-running release provides, fully determined the instant that release is running, with no side effect to sequence. Fixed by unconditionally rewriting it to a hardcoded `_WB_CORE_API_VERSION_CURRENT` on every `wb install`/`wb apply` (`_workbench_sync_version_facts`, `lib/core/version.sh`) — the same discipline D21 already gave `register.list`/`installers.list`, extended to this file. `STATE_SCHEMA_VERSION`'s existing deliberate-migration pattern (`_workbench_migrate_state_schema`) is unchanged and remains the correct approach for any future field that, unlike this one, does assert something about other on-disk state. |
| D37 | Retire dead `MANIFEST_SCHEMA_VERSION` version-file field (found auditing D36's exposure across all four version-file fields) | `MANIFEST_SCHEMA_VERSION` in `~/.config/workbench/core/version` was written on every bootstrap but never read — its getter (`_workbench_manifest_schema_version()`) had zero callers anywhere in the codebase. Real manifest-schema-version enforcement was always a separate, hardcoded `_WB_MANIFEST_SCHEMA_VERSIONS_SUPPORTED="1"` constant, itself already intentionally duplicated between `lib/manifest/parse.sh` and `lib/manifest/validate.sh` (the latter runs standalone and can't source the former). Wiring the version-file copy up to also track that constant would have made it a *third* place a future schema bump has to be remembered, for a field nothing reads — retired instead: no longer written by `_workbench_ensure_version_file`/`version-defaults.conf`, and stripped from any existing host's file by `_workbench_sync_version_facts` (D36) the next time `wb apply` runs. `WORKBENCH_CORE_SEMVER`, the file's fourth field, was audited at the same time and found *not* exposed to this problem — its only consumer is a test asserting it stays pinned at `0.1.0` — so it is deliberately left untouched by both D36 and D37. |
| D38 | Bash-native scheduled-sync timer, opt-in and off by default (host debugging session — scheduled sync never ran on a real host with no ansible-playbook installed) | Confirmed regression: the systemd `--user` timer/launchd agent that fires `wb sync run-if-due` lived entirely inside the Ansible `module_sync` role, reached only when `ansible-playbook` is on `PATH`. On a host without it, scheduled sync silently never ran, indefinitely — `wb sync enable <module>` gave a false-positive "already true — no-op" because it only ever checks the per-module `SYNC_ENABLED` flag, never whether an OS timer actually exists. Fixed by moving timer install into `lib/sync/scheduler.sh` (`workbench_scheduler_install`), independent of Ansible entirely on every platform. Deliberately made **opt-in, off by default**, via a new host-wide `wb scheduler enable|disable|status` command family, persisted in `~/.config/workbench/core/scheduler.conf` — a separate, host-wide layer *underneath* each module's own `SYNC_ENABLED` (`wb sync enable|disable [<name>]`); both must be "on" for a module to ever be touched automatically. No background timer is written to a host's disk, or handed to systemd/launchd, without an explicit one-time `wb scheduler enable`. A host that already had a working Ansible-installed timer before this shipped has that state carried forward automatically, exactly once, the first time `scheduler.conf` doesn't yet exist (`_workbench_scheduler_migrate_existing_install`) — upgrading must not silently stop something a user was already relying on, the same concern in the opposite direction from installing something they never asked for. The Ansible `module_sync` role's now-superseded "Scheduled sync" tasks and `templates/{systemd,launchd}/` were removed; its unrelated module-registration task, and the entirely separate `sync-external` role's own hourly timer for `external_synced_repos`, are untouched. The fixed-5-minute-poll-plus-userspace-self-throttle cadence design itself (`contracts/tracking-spec.md` §Cadence) is unchanged. Ansible remains in place for whatever else it still does — this decision scopes only to the scheduled-sync timer, not a full removal of Ansible from the project. |
| D39 | `wb` shell-function auto-reload after state-changing commands (host debugging session — gpg module's freshly-registered functions confirmed present in `register.list`/`wb functions` but not callable until the shell was restarted) | Confirmed as expected, not a bug: a subprocess (the real `bin/wb` binary invoked by `wb update`/`wb apply`/etc.) cannot alter its own parent shell's functions or environment — Unix process semantics, unrelated to D21's register.list-rendering fix, which this exposed as still requiring a new shell (or a manual `source ~/.bashrc`) to actually take effect in an already-open one. Fixed with the same "activate" pattern tools like nvm/rbenv/direnv use for the identical constraint: `lib/loader.sh` now also defines a `wb()` shell function (alongside everything else it already does on every shell start) that runs the real binary via `command wb` (bypassing itself — no recursion) and then unconditionally re-sources `lib/loader.sh` itself in the *caller's own shell* afterwards, for every subcommand except the purely-informational ones (`status`/`functions`/`tools`/`version`/`help`/`-h`/`--help`/no-args) — deliberately not conditioned on "did anything actually change", mirroring D21's own unconditional-idempotent reasoning, since re-sourcing this file is already idempotent by design (confirmed via `_workbench_register_script_version`'s existing dedupe-by-path, first-registration-wins behaviour) and a future subcommand nobody thought to exclude defaults to the safe direction (one harmless extra reload) rather than the unsafe one (a silently-stale shell again). A new `wb reload` pseudo-command (shell-function-only — `bin/wb` itself has no working case, only one that explains why if reached directly) covers the one case this doesn't: content changed by the background scheduled-sync timer (`wb sync run-if-due`), not something run by hand in the shell that needs refreshing. `WORKBENCH_LOADER_PATH`, exported once near the top of `lib/loader.sh` alongside its other detected/derived facts, is what both the wrapper and `reload` resolve to source — computed the same way `_wb_write_rc_stub`'s rc-file stub line already does, not a new independent computation. See `tests/check-wb-shell-reload.sh`. |
| D40 | Wave C module CI/release model | Two reusable workflows hosted in `workbench-core`, called by each of the eleven module repos' own thin `.github/workflows/*.yml`: `module-ci.yml` (shellcheck, each module's own `tests/check-*.sh`, developer-time manifest validation via `lib/manifest/validate.sh`, and a new "add to core" check exercising real `wb add`/`wb track --branch`/`wb update` against the module's own branch under test — generalising the resolve-ref-under-test idiom D26 established for `check-distribution-no-git.sh`) and `module-release.yml`/`module-release-finalize.yml` (Conventional-Commit release automation, same propose-PR-then-finalize shape as D27/D28, but repo-level version/tag only — no per-file script-local bump, since module repos carry no `_workbench_register_script_version` surface; the version is read back from the `release/v<version>` branch name rather than a repo-local `VERSION` file, since modules don't carry one). Deliberately scoped to "does this one module add correctly to the current core" — no cross-module integration testing, no community-submission validation pipeline (post-Wave-E, still undesigned). Default core ref under test resolves dynamically to `workbench-core`'s latest published GitHub Release (not a fixed `main`/tag), overridable per call via a `core_ref` input for testing against an in-flight core PR branch. The `add-to-core` check always runs with `--allow-hooks` against an isolated scratch `HOME`/`XDG_*`, matching a real first-time user's `wb add`. A module may optionally ship `tests/check-add-to-core.sh` for bespoke verification the shared script can't anticipate; none exists yet for any of the eleven. `RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY` (a repo variable and a repo secret respectively, same GitHub App as D28) must be propagated to each of the eleven module repos individually — no organisation-level secret store on a personal GitHub account — a one-time manual/scripted step, not something this decision's automation can do to itself. `required_status_checks` ruleset activation on the module repos is a deliberate follow-up, not bundled into this decision — it waits on a real green run per repo. See `docs/module-authoring.md`'s "Testing your manifest" section for the mechanics module authors actually need. |
| D41 | `wb tools install` alias + list/lookup UX fix | `wb tools install [<name>]` added as an exact alias of `wb tools update [<name>]` (same function; verb threaded through only so log lines echo back whichever one was typed) — real usage showed `update` doesn't match how someone reads `wb tools list`'s output or thinks about the operation ("I want to install X"), and D23 already made update-in-place the installer function's own job, so aliasing costs nothing. `wb tools list` gains a one-time usage hint (not per-line) pointing at the friendly-name column, not the raw `install-<name>` function name in the right column, as what to pass to `update`/`install`. `workbench_tools_lookup` also accepts the raw `install-<name>` spelling as a fallback, tried only after an exact friendly-name match misses — collision resolution still operates on friendly names only. |
| D42 | Invalid Conventional Commit scope silently drops release severity (found after #34 merged with scope `tools`, which is neither a registered file nor `core`) | `compute-bumps.sh`'s explicit-scope branch only recognises a registered file's exact repo-relative path or the literal `core`; any other scope is logged as a `WARNING: ... names scope '<x>', which is not a registered file or 'core' — ignored` and that commit's severity is dropped entirely — it does **not** fall back to auto-detection the way giving no scope at all does. `pr-check.yml`'s `check-commit-format.sh` only validates Conventional Commit *grammar* (`type[(scope)]: subject`) at PR time, not that `scope` actually names something real, so a component-style scope (`tools`, matching neither `bin/wb` nor `lib/core/tools.sh`, the two registered files #34 actually touched) reaches `main` clean and silently costs that PR its release credit — permanently, since `compute-bumps.sh` walks the same `(<last-tag>, HEAD]` range on every subsequent `release.yml` run and would just log the same warning again, never reconsidering a since-merged commit's severity differently. Recovered here via a single `core`-scoped correction commit carrying the dropped `minor` severity forward, rather than rewriting #34's already-merged history (non-negotiable) or re-bumping `bin/wb`/`lib/core/tools.sh`'s own script-local versions a second time (they were already correctly hand-bumped inside #34 itself, per the existing per-file-version convention `docs/release-process.md` documents separately from `VERSION`). No change made to `compute-bumps.sh` itself — treating an explicit scope as an author override (skip diff-based auto-detection entirely, trust the named path/`core`) rather than falling back when it doesn't resolve is existing, deliberate behaviour (docs/release-process.md's own scope bullets describe the two valid values), just not previously called out as a footgun when the named scope is neither — contributors just need to check a scope actually resolves to a registered path or `core` before using one; when in doubt, omitting `scope` entirely and letting auto-detection attribute severity per touched file is the safer default. |
| D43 | `wb tools install`/`upgrade` split — bare "converge everything" was the wrong model for tools (real host debugging session) | Confirmed design flaw, not a bug in the mechanism D23 built: bare `wb tools update` (and its D41 `install` alias) ran every discovered `install-<n>` function across every loaded module with zero notion of "did the user actually want this tool" — found on a real host where a deliberate, targeted `install-starship` was silently followed, in the same bare invocation, by `install-oh-my-posh`, which then won `workbench-shell`'s prompt-engine election over `starship` purely by load order (that omp.sh-beats-starship-by-load-order election bug is real but belongs to `workbench-shell`, not this repo — deliberately deferred, not fixed here). D23's "converge everything" model is safe for *modules* (`wb update` with no target), since a module is already an explicit opt-in via `wb add`; applying the same model to *tools* — auto-discovered by introspecting installer files, with real side-effecting installs behind each function — was the actual mistake, not anything about the discovery/invocation mechanism itself. Fixed by splitting the single update/install path into two distinct verbs: `wb tools install <n>\|all` now always requires an explicit target (a bare `wb tools install` is a usage error) — `all` lists every discovered tool and requires a plain-stdin `y`/`Y`/`yes`/`YES` confirmation (matching `lib/modules/dev.sh`'s existing plain-`read -r` precedent, not `_read_prompt`'s `/dev/tty` read, since this is a top-level interactive command a human runs directly) before running anything, and anything else — including EOF — aborts with nothing run. A new `wb tools upgrade [<n>\|all]` only ever runs `install-<n>` for a tool whose optional `installed-<n>` predicate — declared in the same already-registered `register.installers[].src` file `install-<n>` lives in, discovered live by sourcing that file on demand exactly like `install-<n>` itself, and never persisted to `installers.list`/bumping `STATE_SCHEMA_VERSION` — reports it as actually installed (exit 0). A predicate reporting exit 1 ("not installed") is dropped from consideration silently; a tool with no predicate declared at all, or one that returns anything other than a clean 0/1, is "unresponsive" — never run, counted, and reported once, since a wrong guess in either direction (skipping something actually present, or installing something that isn't) is worse than an honest "don't know." `wb tools upgrade <n>` applies the same three-way check to a single tool and treats not-installed/unresponsive as a no-op, never an error — only a genuine `install-<n>` failure is. `wb tools list --status` reuses the identical check to annotate each discovered tool's status without invoking anything. `wb tools update` is **dropped outright**, not kept as a deprecated alias — usage was low enough, and the breakage from a hard error pointing at `install`/`upgrade` negligible enough, that carrying it forward as a silent alias for either new verb would just reintroduce ambiguity about which behaviour it means. `all`, `list`, `install`, `upgrade`, and `status` are now reserved friendly-tool-names, excluded from discovery by `workbench_tools_collect` (with a one-time warning) the same way a cross-module collision already is — a module declaring `install-all` would otherwise be indistinguishable from the verb. |
| D44 | `wb` shell completions (bash/zsh) — Phase 1 scope | Completions for `wb` itself live in `workbench-core`'s own manifest (`register.shell[]`, `tier: tools`), not a separate module — the same "completions live with the tool's owning module" pattern already established by `workbench-git`'s `gh.sh`/`glab.sh` and `workbench-devtools`'s `uv.sh`/`terraform.sh`; `wb` is core's own tool. A new `wb completion bash\|zsh` subcommand (`lib/core/completion.sh`) generates the script by introspecting `bin/wb`'s own dispatch case as plain text — `_extract_function_names`'s technique, never sourced — rather than hand-maintaining a parallel command list, so the completion set structurally cannot drift from what `wb` actually dispatches on. Wired into the shell via `shell/completions/wb.sh`, a version-stamped cache keyed on `_workbench_release_version`, following `gh.sh`/`uv.sh`'s existing cache pattern exactly. Phase 1 scope is top-level subcommand completion only — no argument-level completion of module/tool/bundle names, which is a deliberate, separate follow-up decision once this mechanism is proven (same "prove on one thing before batching" pattern as the module CI rollout, D40). Zsh's completion system (`compdef`) may not be initialised on a core-only install with no `workbench-shell`; `shell/completions/wb.sh` defensively runs `compinit -C` itself, guarded on `compdef` not already existing, so it's a no-op when `workbench-shell`'s own `zsh.sh` (or the user's own `.zshrc`) has already done it. |
| D45 | `wb module info`/`wb module docs` — module-published metadata surface | New `wb module` command group with two read-only subcommands. `info <n>` is built mostly from facts core already tracks unconditionally (`sync.conf`'s `REPO_URL`/`PRIVATE`/`TRACK_MODE`/`TRACK_REF`/`RESOLVED_SHA`/`SYNC_ENABLED`/`REGISTERED`, plus the manifest's existing `core_api` scalar) — the only genuinely new, author-supplied field is an optional one-line `info.description` (new `workbench_manifest_info_description()`, `lib/manifest/parse.sh`, same nested-scalar-under-a-block shape `workbench_manifest_sync_enabled()` already established). Deliberately NOT gated behind `core_api` the way `register:` is: `core_api` exists to protect a module's declared shell/tool/getter registrations from running against an incompatible Core API surface — `info:` has no functional consequence on anything the engine does, so there is nothing to gate. `docs <n>` is file-based, not manifest-based — long-form documentation doesn't belong in a YAML scalar read by the deliberately narrow hot-path awk parser (`lib/manifest/parse.sh`'s own header: "not a general YAML parser") — resolved by a fixed, convention-only lookup (`workbench_module_docs_path()`, new `lib/modules/info.sh`) that checks exactly `HELP.md` then `README.md` at the module's `current/` root, no further fallback chain and no manifest field to declare an alternate path. Both commands work uniformly across every tracking mode and visibility (public/tarball, private/branch shallow-clone-and-discard) because both read from `current/`, which is always a real, persisted, `.git`-stripped snapshot regardless of fetch mechanism (D12/D14) — no special-casing needed. Named `docs`, not `help`, specifically to avoid colliding with the CLI's own existing `wb help <command>`/`wb <command> --help` meaning (this CLI's own usage text, not a module's self-description) — `_wb_help_module` calls the distinction out explicitly rather than relying on the name alone. An unpublished field is never an error: both print a plain, non-blaming "not published yet" line pointing back at the module's own repository (already known from `REPO_URL`, independent of anything the module chose to publish) rather than treating a quiet module as a lesser citizen. |
| D46 | Manifest filename — `workbench.yml`/`workbench.yaml`/`wb.yml`/`wb.yaml` as a parallel, version-2 spelling of the manifest, `.dotfiles-sync.yml` unchanged (raised in chat, reopening §5.2/§5.4's "same filename, permanently") | Supersedes the "not a new filename" framing in §5.4, not the compatibility guarantee behind it: `.dotfiles-sync.yml` stays exactly as documented, version 1 only, forever, with zero behavioural change — the `dotfiles` install (frozen at v1.10.0, still live via its unattended timer per the non-negotiables) only ever reads that literal name and never will, so legacy compatibility was never actually at stake. What changes: `version: 2` — previously reserved by §5.4 as a same-filename escape hatch for a genuine schema break — is repurposed as the flag for a *new* filename carrying the identical, already-additive field set (`core_api`/`sync`/`register`, unchanged), discovered as `workbench.yml`, `workbench.yaml`, `wb.yml`, or `wb.yaml` in that precedence order, ahead of `.dotfiles-sync.yml`. Filename and version are now a bound pair, enforced both at the developer-time validator and at the sync engine's own D30 refusal gate: `.dotfiles-sync.yml` must declare `version: 1`, any of the four new names must declare `version: 2` — either mismatch refuses the sync and leaves the previous snapshot live, exactly as an unsupported version already did. Because `wb.yml`/`wb.yaml` are generic enough to plausibly collide with an unrelated tool's own config file, discovery for any of the four new names requires the candidate to declare *some* top-level `version:` key before it's treated as a workbench manifest at all — one with no such key is silently treated as not-ours and discovery moves on to the next candidate, never erroring on a stranger's file; `.dotfiles-sync.yml` is exempt from this sniff-check and stays unconditionally trusted, as it always has been. No forced migration and no deprecation timeline for `.dotfiles-sync.yml` — possibly none, ever, left open deliberately. `workbench.yml`/`version: 2` is now what documentation and quick-start examples lead with for new or migrating repos. `workbench-core`'s own manifest is deliberately left on the legacy name for this change — prove the mechanism on a module first (see `contracts/core-api.md`'s reference example, left untouched). Beyond `lib/manifest/parse.sh`/`lib/sync/engine.sh`/`lib/manifest/validate.sh`, three `bin/wb` call sites that read a module's manifest directly rather than through the engine's helpers (`wb status`'s register.list check, `wb functions`' getter listing, `wb module info`'s core_api/description) were also switched to the same resolver, so those commands don't silently go blind for a module on one of the four new names. |
| D47 | PR #46's squash-merge commit dropped its own release severity — a real incident found immediately after merge, generalising D42's "unparseable/unresolvable commit drops severity silently" finding to the PR *title*, not just individual commit scopes | This repo squash-merges every PR, and GitHub's default squash commit message uses the PR's *title* as the resulting commit's subject — not any of the PR's individual commit messages, which survive only in the squash commit's body. #46's four individual commits were each correctly Conventional-Commit-formatted (`pr-check.yml`'s existing per-commit check passed, as designed), but the PR's own title ("Support workbench.yml/wb.yml as a parallel, version-2 manifest name") was never given a `type[(scope)][!]:` prefix — so the squash commit that actually landed on `main` didn't parse, `compute-bumps.sh` correctly logged its D42-established warning and dropped the merge's severity entirely, and `release.yml` saw nothing to release: no version bump, no CHANGELOG rename, no tag, no GitHub Release, for a real `feat`-level change. Recovered via a single `feat(core):`-scoped correction commit (this decision's own commit) carrying the dropped `minor` severity forward — the same recovery *pattern* D42 established, applied here at the PR-title layer instead of a commit-scope layer; per that precedent, this recovers the overall `VERSION` roll-up only, not a second retroactive bump of the individual registered files' own script-local versions (`lib/manifest/parse.sh`/`lib/manifest/validate.sh`/`lib/sync/engine.sh`/`bin/wb`), which simply stay one release cycle "behind" their true last-changed point — a bookkeeping gap, not a safety one. Unlike D42, this incident is also closed against recurrence: `pr-check.yml` gained a second job, "PR title format" (`.github/scripts/release/check-pr-title-format.sh`, reusing `lib.sh`'s existing `_rel_parse_header`), which fails a PR at PR time if its *title* doesn't parse as a Conventional Commit — the same fail-loud-while-still-fixable principle `check-commit-format.sh` already applied to individual commits, now covering the thing that actually becomes the squash commit's subject. `pr-check.yml`'s `pull_request` trigger is deliberately given an explicit `types: [opened, edited, synchronize, reopened]` (not the unstated default of `[opened, synchronize, reopened]`) — caught in this PR's own review: a title edited after the check last ran, in either direction, would otherwise leave a stale pass/fail result instead of re-validating what will actually become the squash commit's subject, exactly the kind of gap this decision exists to close. `docs/release-process.md` documents the PR-title requirement explicitly alongside the existing per-commit/scope rules. |
| D48 | Module-shipped, user-editable opinionated defaults (`overrides_src`) — raised in chat, closes the gap `set-omp-theme-permanent` (workbench-shell) worked around by hand | A module's answer to "I ship an opinionated default, but a user should be able to change it without editing a file inside my immutable per-sync snapshot" (§12 D16). New optional top-level manifest scalar, `overrides_src: <path>`, validated exactly like `deploy[].src` (safe relative path) — deliberately no `dest` field, for the same reason `register.shell[]` has none (§12, `register:`): the destination is always engine-computed, never author-specified, because this file lands inside `~/.config/workbench/local/` — the one directory a user's shell sources unconditionally on every start, and the one place the project's non-negotiables specifically forbid a module from choosing its own path into. Engine deploys it once, to `${XDG_CONFIG_HOME:-~/.config}/workbench/local/overrides/<module-name>.sh` (`<module-name>` is the `wb add`-addressable registration name), reusing `deploy[]`'s existing `copy` semantics with `force` hardcoded `false` — there is no `force: true` escape hatch for this field; once deployed, the file is the user's, permanently. A module bumping `overrides_src`'s content in a later release does not touch a machine that already has the file — same one-way-publish model `copy` mode already documents. Deliberately no auto-scaffolded blank placeholder for a module that doesn't have `overrides_src` yet: a module simply omits the field until it has a real opinion worth exposing, avoiding both a content-hash-based idempotency mechanism the engine has nowhere else (existence-based `force` is the only idempotency primitive it has, by design — dependency-light) and core pre-creating files on behalf of modules it knows nothing about (principle 4). `lib/loader.sh` sources `${WORKBENCH_LOCAL_DIR}/overrides/*.sh` early — before every module's own tier content runs, and before `~/.config/workbench/local/settings.sh`'s own early pass — so a variable a module's shipped default sets is visible to that module's own `${VAR:-default}`-style tier code, and a user's `settings.sh` still wins if it sets the same variable. New module-authoring convention, `WORKBENCH_OVERRIDE_<SCOPE>` (`docs/module-authoring.md`): reserved for opinions with no existing override path today — specifically **elections**, where a module hardcodes a priority between two or more mutually-exclusive implementations of the same feature (e.g. workbench-shell's oh-my-posh > starship > oh-my-zsh guard clauses) via bare `command -v` checks with nothing for a user to set. A default that already has a natural `${VAR:-default}` escape hatch (`OMP_THEME`) needs no renaming — the convention exists for opinions that currently have no escape hatch at all, not as a blanket rename of every existing knob. Retrofitting workbench-shell's own election guards to use this convention is explicitly out of scope for this decision — a separate, immediately-following brief. |
| D49 | `module-ci.yml`'s `manifest-validate` job never actually exercised D46's discovery — found while migrating `workbench-shell` to `workbench.yml`, the first module to try it | D46 shipped `lib/manifest/validate.sh` with full filename discovery (workbench.yml/workbench.yaml/wb.yml/wb.yaml/.dotfiles-sync.yml, in precedence order) for exactly this "a module repo's own CI, standalone" use case, but the reusable `module-ci.yml` workflow that every one of the eleven module repos' `pr-check.yml`/`ci.yml` actually calls was never updated to use it — its `manifest-validate` job still invoked `_core/lib/manifest/validate.sh .dotfiles-sync.yml` with the legacy filename hardcoded as an explicit argument. `validate.sh`'s own usage text is unambiguous that an explicit path argument always bypasses discovery, so this wasn't a latent bug exposed by chance — it meant D46 had never actually been exercised end-to-end in CI for any module, migrated or not, until `workbench-shell`'s manifest-rename PR hit it. Fix is the minimal one: drop the hardcoded argument so `validate.sh` runs its own discovery against the module's checkout (already at the workflow's default path, no `path:` override), exactly as D46 intended. Verified both directions locally against a real module checkout: discovers `workbench.yml`/`version: 2` correctly, and still discovers `.dotfiles-sync.yml`/`version: 1` unchanged for the ten module repos not yet migrated — no regression for either spelling. No decision to revisit here beyond the fix itself; D46's design was correct, only its CI wiring lagged it. |
| D50 | Prompt-engine state doesn't reset on reload — real host report: `WORKBENCH_OVERRIDE_PROMPT_ENGINE` (D48/workbench-shell retrofit) changed correctly on disk, but neither `wb apply` nor `source ~/.bashrc` moved the visible prompt, only a full shell restart did | Reproduced against a live clone of `lib/loader.sh`: re-sourcing this file in an already-running shell (`source ~/.bashrc`, `wb reload`, or the `wb()` wrapper's own auto-reload, D39) correctly re-reads an edited overrides file and correctly re-runs election — `WORKBENCH_PROMPT_ENGINE` itself flips — but nothing ever tore down the *previously*-elected engine's own shell hook. Re-sourcing a file in a running shell is not a new process, so anything a prompt engine hooked into the shell to render itself (bash's `PROMPT_COMMAND`, zsh's `precmd_functions`) is still live from the last run; well-behaved prompt tools (starship's documented bash init is exactly this shape) *preserve* whatever `PROMPT_COMMAND` already contains rather than overwriting it, so they can coexist with unrelated tools — correct behaviour for coexistence, exactly wrong the moment one tool is meant to *replace* another. Fixed once in `lib/loader.sh`, not per-module: immediately after the `WORKBENCH_TRACK_<MODULE>` block and before the tier loop, `WORKBENCH_PROMPT_SET`/`WORKBENCH_PROMPT_ENGINE` are unconditionally unset, and — only when `WORKBENCH_PROMPT_SET` was already true, i.e. only on an actual reload where this loader is known to be what last touched the hook, never on a genuinely first load where either variable could hold a user's own pre-stub customisation — bash's `PROMPT_COMMAND` is `unset` outright (not blanked; bash 5.1+'s internal array representation means `PROMPT_COMMAND=""` doesn't reliably clear every element) and zsh's `precmd_functions` is reset to `()`. This gives whichever engine wins a clean slate every reload, the same state a brand-new shell would have, so a prompt-owning module's own guard never needs to defensively clear a competing engine's leftover hook itself (documented as part of the prompt-ownership convention, `contracts/core-api.md`). Deliberate trade-off: a module that appends to `PROMPT_COMMAND`/`precmd_functions` for its own unrelated purposes from within tier content also gets cleared on the next reload if `WORKBENCH_PROMPT_SET` happened to be set — but the same tier content re-runs every reload regardless, so it's re-added, not lost. Verified with two synthetic append-and-preserve fixture modules (real `oh-my-posh`/`starship` weren't installable in the sandbox that produced this decision; the fixtures model the specific documented behaviour, not a guess) — confirmed no stale hook survives a switch, correct election either way, and a genuinely first-ever load with no module electing at all leaves a user's own pre-stub `PROMPT_COMMAND` customisation untouched. See `tests/check-prompt-reset-on-reload.sh`. Not yet confirmed against the real `oh-my-posh`/`starship` binaries — flagged as the one piece of this fix that still wants that confirmation. |
| D51 | "Get me back to the workbench default" — `wb module reset` | User-editable copy-mode deploy[] files (starship.toml, direnv.toml, and — once the follow-on workbench-shell change lands — tmux.conf/vimrc) had no way back to the shipped default once a local edit went wrong, short of deleting the file and re-syncing (which doesn't even work for `force: false` entries, since a missing dest is exactly what triggers a fresh copy — the *file* comes back, not necessarily today's upstream content, and nothing about that path is discoverable). `wb module reset <module> <name>|all` force-redeploys one or every copy-mode deploy[] file from the module's current snapshot, bypassing `force: false` for that one call — the manifest's own `force` setting is untouched for every future ordinary sync. `<name>` matches by the basename of the deployed dest (`starship.toml`, `.vimrc` — note the leading dot on anything deployed under a literal dotfile name, since basename doesn't strip it); a basename collision across two deploy[] entries falls back to matching the full `src` path instead, and lists both forms if that's still ambiguous. `mode: link` entries are out of scope — they're already kept in sync on every update, so there's nothing to reset. A directory `src` is also out of scope for now (documented limitation): resetting an entire tree by one name is a different, more ambiguous operation than a single file, and no module currently needs it. Always confirms before overwriting — both a single named target and `all` — a deliberate asymmetry from `wb tools install`'s own confirmation rule (§12 D43, where only `all` confirms, on the theory that naming one target there is itself the deliberate act): this command destroys real local edits even when the name given is exactly right, so a typo that still happens to match a valid file is a materially worse outcome here than installing one tool too many. Surfaced, and fixed as a prerequisite rather than a separate follow-up, a real bug in `workbench_deploy_copy_file`: `[[ -e "${dest}" && "${force}" != "true" ]]` treats a symlink as "existing" (correct), but when `force=true` the subsequent `cp -f "${src}" "${dest}"` follows a symlink destination and overwrites whatever it points at, not the symlink itself — for a stale `mode: link` destination (exactly what tmux.conf/vimrc's migration produces on every host that already has this module installed), that target is the module's own immutable per-sync snapshot file, silently corrupted, while the destination is left still a symlink afterwards. Reproduced directly (a real `cp -f` over a real symlink, not a reasoned-about hypothetical) and fixed with `[[ -L "${dest}" ]] && rm -f "${dest}"` immediately before the `cp` — this protects every caller of `workbench_deploy_copy_file`, not just the new command, including the ordinary sync path for any existing manifest that ever sets `force: true` on a copy-mode entry today. `workbench_module_reset_targets`, the new listing/lookup helper, deliberately re-derives the platform-filter/macOS-dest-override/`~`-expansion logic already present in `workbench_deploy_module`'s own loop rather than refactoring that loop to share it — duplicating roughly six lines was judged lower-risk than touching the tested, hot sync path; the two must be kept in sync by hand if either changes. Two review findings on the implementing PR, both confirmed real and fixed before merge: (1) the full-src-path disambiguation for a basename collision was unreachable as first written — the fallback re-filtered the *already basename-matched* set by comparing each entry's `src` against the typed target, but a full src path never equals a bare basename, so the basename-match pass that had to run first always found zero matches and returned the "doesn't match any" error before the disambiguation branch was ever reached; fixed by tracking a src-path match independently, in the same pass, so it resolves whether or not a basename collision exists. (2) `workbench_deploy_copy_file` always returned 0 — its last statement was `log_info`, so a failed `cp -f` (permissions, a vanished src, disk full) was silently swallowed and `wb module reset` reported success regardless; fixed by returning `cp`'s own exit status, and `_wb_cmd_module_reset` now tracks per-file failures across an `all` reset and exits non-zero, naming the failure, if any one file's copy failed, rather than reporting blanket success. |
| D52 | `sudo-test`/`get-elevation-command` crashed on unset `$USER` (Docker container testing) | Both referenced `${USER}` directly; confirmed unset in a bare `docker run -it fedora:latest` root shell (no login/PAM session ever exports it). Under `bin/wb`'s `set -uo pipefail`, get-elevation-command's reference inside elevate-cmd's `elevation_cmd="$(get-elevation-command)"` command substitution killed that subshell with "unbound variable" — swallowed by elevate-cmd's `|| return 1` — so `workbench_install_shell_prereqs` silently never invoked the package manager, surfacing only as a generic "could not install all missing prerequisites automatically" warning with no indication why. Fixed via a new private helper, `_workbench_current_user` (`id -un`), which both functions resolve into a local variable instead of referencing `${USER}` directly. Purely an implementation-detail fix to two already-documented Core API functions — no `CORE_API_VERSION` bump. See `tests/check-prereqs.sh`. |
| D53 | Backup-before-overwrite for force copy/link deploys (raised in chat, closing the gap `wb module reset`/D51 shipped with — "cannot be undone" had no backup behind it) | `workbench_deploy_copy_file`'s force branch and `workbench_deploy_link_file`'s force branch (the latter found while reading this code for the former — its `rm -rf "${dest}"` on a real file *or an entire real directory*, with zero backup, is the same gap, one level more destructive, and no live manifest happens to trigger it today) both now call a new shared `workbench_backup_existing_file <dest> [<module>]` immediately before destroying anything real. Deliberately no confirmation prompt for the backup step itself: the same force-copy path already runs unattended off the sync timer (today, `workbench-git`'s three `force: true` deploy entries, redeployed whenever that module's tracked commit changes) — a blocking `read` there would hang a background systemd/launchd run with no one to answer it, so "default-safe, no prompt" is the only shape that behaves identically whether the caller is a human running `wb module reset` or the timer. Backup root is `${XDG_DATA_HOME:-~/.local/share}/workbench/backups/<YYYY-MM-DD>/<HH00>/<module>-<basename>.<HHMMSS>` (`contracts/state-schema.md`'s new "Backup root" section) — a sibling of the existing `modules/` state root, same engine-computed-destination posture as everything else under that root. Copy-mode backup is skipped when the incoming content is byte-identical to what's already at `dest` (`cmp -s`), so an unchanged `force: true` re-deploy of module-owned, no-user-content files never accumulates backups with nothing new in them; link-mode backup is unconditional regardless of content match, since replacing a real path with a symlink is a type change worth preserving on its own. A failed backup (disk full, permissions) aborts the overwrite rather than proceeding unprotected — a silently-skipped backup is a worse outcome than refusing the operation. No retention policy or cleanup schedule, by design, matching the anticipated low volume — copy-mode entries only re-run when a module's upstream commit actually changes (the sync engine already gates the whole deploy step on that), and `wb module reset` is a manual, occasional command. Notification is `log_info` only (already unconditional to stderr, per `lib/core/log.sh`) — no new `wb status` surface; `_wb_cmd_module_reset`'s pre-confirmation text now also names the backup root and drops "This cannot be undone," which stopped being accurate the moment this landed. `workbench_deploy_copy_file`/`workbench_deploy_link_file` both gain an optional trailing `module` argument (backward-compatible — `${4:-}` — nothing else calling either function needs to change); every call site inside `workbench_deploy_module` and `_wb_cmd_module_reset` now passes it through. See `tests/check-module-reset.sh`. |
| D54 | `wb` shell completions (bash/zsh) — Phase 2 scope | Extends D44's Phase 1 (top-level commands only) with each command group's own sub-command completion (`wb tools <TAB>` → `list install upgrade`, `wb module <TAB>` → `info docs reset`, `wb sync <TAB>` → `enable disable run-if-due`, `wb scheduler <TAB>` → `enable disable status` — the last two introspected via a new, generalised `_wb_completion_subdispatch_commands <function-name>`, scoped to the named function's own body first since `bin/wb` has four separate `case "${sub}" in` blocks with identical opening text; extended to handle `_wb_cmd_sync`'s pipe-combined `enable\|disable)` arm alongside the other three sub-dispatchers' one-word-per-arm style, rather than a second code path for that shape) and argument-level completion for registered module names (`track`/`remove`/`dev`/`update`/`sync enable\|disable`/`module info\|docs\|reset`), catalog module names (`add`), catalog bundle names (`install --bundle`), and discovered tool friendly names plus the literal `all` (`tools install`/`tools upgrade`) — via `workbench_list_registered_modules` (deregistered modules are deliberately never offered — only what a command would actually accept without erroring), two new catalog enumeration functions (`workbench_catalog_list_modules`/`workbench_catalog_list_bundles`, `lib/modules/catalog.sh`, same override-aware file resolution as the existing lookup functions), and `workbench_tools_collect`. Unlike the static top-level/sub-command lists (introspected once at generation time and baked into the cached script, since they never change between releases), these name lookups are queried fresh on every completion via a new hidden `wb __complete <kind>` dispatcher (`_wb_cmd_complete`, `lib/core/completion.sh`) — the same "shell out to the real binary for live state" pattern already established by `get-functions` delegating to `bin/wb functions` (`lib/core/functions.sh`), and standard practice for dynamic bash/zsh completion generally. `__complete` never appears in `wb <TAB>` itself or any help text — an identifier starting with `_` can't match D44's own `[a-z]`-leading dispatch-introspection regex, so no separate exclusion list is needed to keep it hidden. `_wb_cmd_complete` never logs on an unrecognised kind (silent empty output only) — anything written to stderr mid-completion would corrupt what's shown while typing. `wb sync`'s offered sub-commands deliberately include `run-if-due` despite it being "not normally run by hand" (its own help text) — introspecting it honestly was judged better than a second, hand-maintained exclusion list that could drift. `_wb_completion_subdispatch_commands`'s case/esac regex requires leading-whitespace tolerance (`^[[:space:]]*case ...`/`^[[:space:]]*esac$`) unlike the top-level dispatch's own anchored-at-column-0 regex: the top-level `case "${_wb_cmd}" in` in `bin/wb` sits outside any function body at column 0, but each sub-dispatcher's `case "${sub}" in`/`esac` is inside a function body and so is indented — matching column-0-only, as an initial draft of this regex (mirroring the top-level one) did, silently matched nothing. Also updated `tests/check-wb-completion.sh`'s Phase 1 command-list-drift check to scope its `compgen -W` extraction to the first (top-level) occurrence only, via `head -n 1` — Phase 2's generated script now emits several more `compgen -W` calls further down for sub-command/argument completion, and the original extraction, un-scoped, swept all of them into what it compared against the top-level dispatch list. Deferred to a follow-up Phase 3, not started here: flag-name completion (`--private`, `--bundle`, `--latest`/`--branch`/`--tag`/`--commit`, etc.) and `wb module reset <name> <target>`'s own target argument, which needs that module's deploy[]-derived reset targets read fresh per module rather than a flat, host-wide list. One review finding on the implementing PR, confirmed real and fixed before merge: every dynamic-argument arm in `_wb_completion_generate_zsh` originally passed `_describe` a literal, already-expanded `"($(wb __complete ... ))"` string as its second argument, but `_describe` resolves that argument via indirect parameter expansion (`${(P)...}`) — it must be the *name* of an array variable, not a value — so every one of those arms silently offered nothing under real zsh (the static sub-command arms, which already assigned to a named `local -a` array first, were unaffected). Fixed by building each dynamic arm's candidates into a named array first (`_wb_reg_mods=(${(f)"$(wb __complete registered-modules 2>/dev/null)"})`, etc.) and passing that array's name to `_describe`, matching the sub-command arms' own shape; verified directly against a real zsh 5.9 interpreter (not just read), confirming both the original failure mode and the fix. See `tests/check-wb-completion-args.sh`. |
| D55 | 'core' scope silently drops a touched registered file's own bump (PR #58/#59) | Generalises D42's "unresolvable scope drops severity silently" finding to the case where the scope *does* resolve — to `core` — but the commit or PR also touches a registered file. `compute-bumps.sh` treats an explicit scope as an author override and, per its documented, deliberate design (D27: "`core`-scoped commits (no corresponding file) can independently drive the overall `VERSION` bump"), never walks a `core`-scoped commit's touched files. PR #58's single commit (`feat(core): backup-before-overwrite for force copy/link deploys`) touched two registered files, `bin/wb` and `lib/sync/engine.sh`, but its `core` scope meant neither's own script-local version was bumped — only the overall `VERSION` moved (2.5.1 → 2.6.0 in PR #59), leaving both files silently stale. Closed at PR time, the same "fail loud while it's still fixable" principle D42/D47 already established: `check-commit-format.sh` now fails any individual commit scoped `core` that also touches a registered file, and `check-pr-title-format.sh` (extended to accept an optional `<base-sha> <head-sha>` diff range) applies the identical rule to the PR *title* against the PR's overall diff — necessary because the squash commit that actually reaches `compute-bumps.sh` only ever carries the title's scope, per D47. `compute-bumps.sh` also gained a visible warning for the same condition, as a post-merge safety net for the admin-bypass path D28 leaves open — it does not change D27's `core` behaviour, only surfaces it. `docs/release-process.md`'s "core-scoped commits: when to reach for one" section already documented this exact rule ("core is the exception... for anything that's really about one file's... behaviour, use auto-detection or an explicit path scope instead") — the gap was enforcement, not guidance, which is why an agent (Claude Code) still got it wrong in practice. A companion Claude Code skill (`.claude/skills/conventional-commits/SKILL.md`) makes the same rule the front-door an agent checks before drafting a commit message or PR title, rather than relying on it re-deriving the rule from `docs/release-process.md`'s full narrative each time. Recovery of `bin/wb`/`lib/sync/engine.sh`'s own stale versions is tracked separately, not part of this decision. |
| D56 | Docs reorg | `ARCHITECTURE.md` moved to `docs/architecture.md`; the decisions log split out into its own file, `docs/decisions-log.md`, referenced from `architecture.md` §12 rather than embedded. |
| D57 | Module repos had no PR-title format check — same D47 hazard, unhardened | `workbench-core`'s own pr-check.yml gained a "PR title format" job under D47 (PR #46: a correctly-formatted commit inside an unparseable-titled PR still lands on `main` as a single unparseable squash commit, silently dropping that merge's release severity). Reviewing D55's core-scope hardening for module-repo applicability surfaced that `module-pr-check.yml` — the reusable workflow all eleven `workbench-*` module repos call — never received the equivalent check, despite module repos squash-merging the same way core does (confirmed against `workbench-git`'s actual commit history: linear, single-parent commits with `(#N)`-suffixed subjects). D55 itself does not port: `compute-bump.sh` (module version) never resolves a commit scope at all (D40 — no per-file registered-script convention to bump), so there is no scope-vs-registered-file case to guard against. What does port is the PR-title grammar check: `.github/scripts/module-pr-check/check-pr-title-format.sh` (a grammar-only simplification of core's script — no base/head diff, no scope rule) plus a new `pr-title-format` job in `module-pr-check.yml`. Because module repos consume this workflow by reference (`uses: GingerGraham/workbench-core/.github/workflows/module-pr-check.yml@<ref>`), this is a single change in `workbench-core` rather than eleven per-repo edits — consistent with the "one generalised engine, not per-module special-casing" principle (§1). `required_status_checks` ruleset activation on module repos (whether any already require "commit message format" and so should also require this new "PR title format" check) could not be verified or updated in this pass — no `overview.md` documenting rollout state exists in this repo, and modifying a live GitHub ruleset needs GitHub admin access this pass didn't have; left as a follow-up. Also flagged, not fixed here: module repos' own thin `pr-check.yml` (e.g. `workbench-git`'s) triggers on the unstated default `pull_request` types (`opened`/`synchronize`/`reopened`), omitting `edited` — unlike core's own `pr-check.yml`, which explicitly adds `edited` per D47 so a title changed after the check last ran is re-validated. Module repos carry the same gap core closed in itself; each module's own `pr-check.yml` would need the same explicit `types:` list, which is per-repo, not something this reusable-workflow-only change can fix. |
| D58 | Module-repo agent-instruction topology + conventional-commits skill | Ported core's D32 topology (`AGENTS.md` canonical, `CLAUDE.md` importing it, `.github/copilot-instructions.md` a maintained duplicate) to all eleven `workbench-*` module repos, plus the module-scoped `conventional-commits` skill flagged as an open question in D57's session. Two simplifications vs. core: no second `.github/instructions/*.instructions.md` split (module repos carry no local `.github/scripts/**` — that surface lives only in `workbench-core`, checked out at CI-time), and cross-repo `https://github.com/GingerGraham/workbench-core/blob/main/...` links in place of core's local relative paths, since the pointed-at docs (`docs/architecture.md`, `docs/decisions-log.md`, `docs/module-authoring.md`) live in a different repo. Content is near byte-identical across all eleven modules — only each repo's title line differs — with the canonical copy held in `docs/module-authoring.md` rather than duplicated across eleven sets of tribal knowledge. Proved on `workbench-git` first, per the usual rollout pattern, before batching the remaining ten. |
| D59 | CI security scanning | Three checks added to every PR against `workbench-core` and the eleven module repos: `gitleaks` (secrets), `clamav` (malware signatures), and a maintained dangerous-pattern grep (`.github/actions/scan-patterns`). Shipped as composite actions in `workbench-core` (`.github/actions/scan-secrets`, `scan-malware`, `scan-patterns`), called from both its own `ci.yml` and the shared `module-ci.yml`, rather than a nested reusable workflow, to avoid any reusable-workflow nesting-depth question (a module repo's `ci.yml` already calls `module-ci.yml` cross-repo; a second reusable-workflow hop from inside it would be a second level, while a composite action reference has no such limit). Deliberately does not extend to third-party/community modules — no submission pipeline exists (see `SECURITY.md`'s "Automated PR checks" section, appended alongside its existing vulnerability-reporting content rather than replacing it — that content predates this change and stays current). Semgrep was considered and rejected for the dangerous-pattern layer: Bash is still Experimental-tier in Semgrep's language support, and a maintained explicit pattern list is more honest about actual coverage than a SAST tool with immature language support would be. Proved on `workbench-git` first (pilot branch pointing both its `module-ci.yml` call and the three new `scan-*` action refs inside `module-ci.yml` at the feature branch, with a throwaway EICAR string and a `curl \| bash` line committed and reverted to confirm each scanner actually fires), per the usual rollout pattern — the other ten module repos need no changes since their `ci.yml` already calls `module-ci.yml@main`. |
